export interface Create {
  construction_id: string;
  period: string;
  weather: string;
  condition: string;
  description: string;
}
export interface Update {
  construction_id: string;
  period: string;
  weather: string;
  condition: string;
  description: string;
}
